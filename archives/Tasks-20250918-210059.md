# Tasks.md

- [x] `pyproject.toml` の `project.name` が `clickutils` になっていることを確認する。
- [x] `src` ディレクトリ以下のパッケージ名が `clickutils` になっていることを確認する。
- [x] `src/clickutils/__main__.py` のパッケージ参照が正しいことを確認する。
- [x] `src/clickutils/clickutils.py` のパッケージ参照が正しいことを確認する。
- [x] `src/clickutils/__init__.py` のパッケージ参照が正しいことを確認する。
- [x] `samples` ディレクトリ内のサンプルコードのインポート文が正しいことを確認する。
- [x] `tests` ディレクトリ内のテストコードのインポート文が正しいことを確認する。
- [x] 全ての確認結果を要約し、矛盾がないことを報告する。
